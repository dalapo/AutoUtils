package dalapo.autoutils.logging;

import cpw.mods.fml.common.FMLLog;
import dalapo.autoutils.reference.NameList;

public class Logger {
	public static void log(LogLevel level, Object object)
	{
		FMLLog.log(NameList.MODNAME, level, object.toString(), object);
	}
}
