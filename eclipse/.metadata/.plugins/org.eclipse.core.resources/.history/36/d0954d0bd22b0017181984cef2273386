package dalapo.autoutils.logging;

public enum LogLevel {
	OFF(0),
	FATAL(1),
	ERROR(2),
	WARN(3),
	INFO(4),
	DEBUG(5),
	TRACE(6),
	ALL(Integer.MAX_VALUE);
	int level;
	private LogLevel(int l)
	{
		level = l;
	}
}